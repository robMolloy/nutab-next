import { VideoStreamControls } from "@/modules/videoStream";

const Parent = () => {
  return (
    <main className={`min-h-screen`}>
      <VideoStreamControls />
    </main>
  );
};

export default Parent;
